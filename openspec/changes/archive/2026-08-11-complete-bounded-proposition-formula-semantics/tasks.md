# Tasks：完成有界命題公式語意

## 1. RED：先固定可觀察契約

- [x] 1.1 依「Goals」與「Non-Goals」建立 change-scope 驗收矩陣：只涵蓋單一 opaque `PropositionExpression`、classical／supervaluation／trace／NOR 能力，明列不加入量化、SAT、第二套 `Formula`、strong Kleene、constant AST case 或 caller-adjustable limits；驗證：逐項把 proposal capability、18 個 Requirement 與既有 #205／#212／#213 回歸 locator 對到後續 RED 測試，且矩陣沒有超出 Non-Goals 的 production symbol。

- [x] 1.2 [P] 先在 `ExpressionConstructionTests` 與 non-`@testable` `swiftc` fixture 寫出「Requirement: Proposition expressions SHALL represent bounded structural negation」及「Requirement: Truth-bearing proposition operations SHALL reject malformed syntax」的 RED：六種 factory、structural identity、含canonical atom-table payload的hash feed、64 depth／4,096 nodes／63 atoms邊界、malformed atom原始錯誤、raw storage與caller limit不可見，以及deprecated `maximumOperatorDepth` safe alias；驗證：新assertion在現有public recursive enum／缺少binary factory時依預期失敗，compile fixture精確證明尚可繞過或尚未提供的新邊界。

- [x] 1.3 [P] 先寫 Unicode、資源與序列化 RED，完整覆蓋「Requirement: Atom order, row order, and canonical bytes are deterministic」及「Requirement: Classical enumeration and expansion use fixed hard bounds」：raw／streaming normalized UTF-8 4,096接受、4,097停止、2,048個U+0344 expansion、unsupported-before-later-overflow、五份Unicode 15.1.0官方inputs／White_Space／NormalizationTest、manifest／generator／runtime-normalizer／generated-table source獨立SHA與offline regeneration、production平台API static ban、四個surface exact hex、六個node tags／非對稱children／rewrite+synthesis representative、tag／length collision pair與fresh subprocess replay；驗證：缺少pinned trust chain、capped sink、canonical codec或bounded `displaySafe`時依預期RED，而非由Foundation／CoreFoundation normalization／whitespace偶然通過。

- [x] 1.4 [P] 先在 `ClassicalSemanticsTests` 寫出「Requirement: Classical evaluation uses a complete bivalent assignment」、「Requirement: Binary connectives have complete four-row matrices」、「Requirement: Semantic equivalence is distinct from structural equality」、「Requirement: Tautology and contradiction are defined by complete truth tables」及「Requirement: Classical formula semantics SHALL be total, bivalent, bounded, and deterministic」的 RED；驗證：entry-list valuation先count／validate再index、duplicate拒絕且無raw dictionary API、not／and／or／implies／nor全矩陣、missing canonical payload、extra assignment、63／64 valuation container、12／13／63 atoms由shared checked-power helper在shift closure與allocator皆為0時拒絕、雙重否定／implication／De Morgan equivalence與tautology／contradictionfixture都先因production API或行為尚缺而失敗。

- [x] 1.5 [P] 先在 `SupervaluationTests` 寫出「Requirement: Evaluation SHALL preserve open-world uncertainty」的 RED：unknown `p`的excluded middle／contradiction／self-implication／NOR、known-unknown absorbing cases、unknown `p,q` implication、12 unknown的4,096 completions與第13個unknown的guard-before-shift；驗證：以atomic observer、checked-shift closure與workspace allocator spy證明預期契約目前尚未成立，且RED可區分bounded supervaluation與strong Kleene。

- [x] 1.6 [P] 先為既有與新增 suites 寫出「Requirement: Valuation outcomes SHALL retain context and structured evidence」、「Requirement: Negation evaluation SHALL derive a recursive evidence trace」、「Requirement: Yes-no questions SHALL expose a tri-valued answer space」、「Requirement: Fact acceptance SHALL be gated from recorded assertions」及「Requirement: Denied stance SHALL remain distinct from asserted negation」的 RED；驗證：`implies(and(p,p),nor(q,not(p)))` 每個 occurrence、雙 child 不短路、snapshot／valid day／quarantine／refusal 保留、question 預留 `not(subject)`、no-answer 不重評、structural adjudication gate 與 denied 不轉 negation 均有會先失敗的精確 assertion。

- [x] 1.7 [P] 先在 `TruthFunctionSynthesisTests` 寫出「Requirement: Every expression has a bounded deterministic NOR rewrite」、「Requirement: BooleanFunctionTable represents bounded finite functions and guarantees binary NOR synthesis」及「Requirement: Classical equivalence and NOR transformation SHALL preserve bounded truth conditions」的 RED；驗證：五種rewrite exact structure／bytes、13...63 atoms的structural verifier、rewrite／synthesis depth65 typed estimator、0-arity table／synthesis refusal、caller bit-order permutation、16個binary masks、三原子`{1,3,6}` non-symmetric multirow與minterm／constant structural goldens、rewrite 4,095／4,097與synthesis 4,095／4,103邊界，以及兩個module-internal per-call fault injectors分別觸發verification failure皆先因production行為尚缺而失敗。

## 2. GREEN：完成 bounded production semantics

- [x] 2.1 依「Decision 2：以固定、不可提高的 library budgets 定義可接受公式與運算」、「Decision 3：資源、classical 與 transformation 失敗使用分層 typed errors」及「Required construction and failure order」，在`Proposition.swift`、runtime `UnicodeNormalizationV1.swift`、generated `Generated/UnicodeNormalizationTablesV1.swift`、`Models.swift`與必要的`Package.swift`整合五份vendored Unicode 15.1.0 inputs、offline generator、完整SHA-256 trust-chain manifest、pinned White_Space、raw early-exit與streaming normalized byte cap、source-order assigned-scalar gate、分層typed errors、最多5 atoms／2,048 scalars的安全診斷及不先全量計數的`displaySafe`；驗證：1.3的官方conformance、offline replay、domain-version mutation、byte-order、U+0344、unsupported precedence與adversarial rendering全綠，machine payload仍保留完整63 atoms。

- [x] 2.2 依「Decision 1：以單一 opaque `PropositionExpression` 承接所有公式」重構`Expression.swift`為private recursive storage的public struct，實作六個throwing factories、read-only kind／proposition／ordered children、cached depth／node／canonical atoms、checked merge、syntax-sensitive equality與先餵canonical atom table再餵node components的`structuralHashFeed`，保留deprecated throwing `makeAtom`／`makeNot`及read-only `maximumOperatorDepth` safe alias並移除nonthrowing`.expression`；驗證：1.2的factory、exact-boundary、child-order、double-negation、atom(p)／atom(q) feed差異與external compile probes全綠，任何candidate error都不留下partial storage。

- [x] 2.3 依「Decision 4：classical semantics 是獨立、完整的 bivalent layer」與「Decision 5：atoms、expressions、rows 與 bytes 使用單一 deterministic canonical order」，在`ClassicalSemantics.swift`實作entry-list `ClassicalValuation`、bounded duplicate rejection、完整Bool evaluation、truth table、equivalence、tautology／contradiction、caller-order-permuting`BooleanFunctionTable`與唯一shared checked-power helper，並以UInt64 big-endian framing、四個獨立domain、固定predicate／reference／六node tags、atom-table index與canonical row index實作bytes；驗證：1.4全矩陣、extra／missing／duplicate assignments、canonical union、12-row-boundary、NFC／NFD equality、all-tag／nested／rewrite+synthesis goldens、collision fixtures及subprocess replay全綠，且兩種table因domain不同而raw bytes不同。

- [x] 2.4 依「Decision 6：epistemic partial semantics 採 bounded supervaluation」在`Projection.swift`建立只以validated expression atoms為key的canonical distinct-atom cache、每atom每次root evaluation只求值一次、false-before-true compatible completions、stable preorder node IDs、iterative postorder observations與`supervaluationInconclusive(atoms:)`，並共用checked-power helper；驗證：1.5所列matrix全綠，12 unknown正好列舉4,096次，第13個unknown在shift closure、allocation與operator trace前以typed error終止。

- [x] 2.5 依「Decision 7：trace 是 exact expression occurrence 的唯讀稽核樹」完成 `EvidenceTrace`、`AtomicEvidenceTrace.refusal`、`SupervaluationSummary` 與 `Valuation` 的封閉 constructors和 iterative equality，讓每個 syntax occurrence保留 exact expression／context／ordered children／atomic evidence或completion summary；同步讓 `Question.swift` 由 subject valuation導出 yes／no／undetermined及 reserved negation，不重投射 atom；驗證：1.6 的 occurrence-count、duplicate-atom-once、兩邊 evidence、不短路、context identity、question no-answer／double-negation、assertion與adjudication identity gate全綠，`EvidenceTrace` 仍只有 `Equatable` 而非 `Hashable`。

- [x] 2.6 依「Decision 8：NOR rewrite 是 bounded structural transformation」在`TruthFunctionSynthesis.swift`先用checked／saturating postorder計算完整rewrite depth／node plan，再依固定identity materialize atom／nor tree，最後以不列舉truth rows的獨立iterative verifier檢查rule、node tag、child order、atom identity與`isNorOnly`；verifier只接受module-internal per-call fault injector且public固定no-op；驗證：五種connective等價、13...63 atoms可rewrite、depth65、4,095成功／4,097拒絕、exact `nor(p,p)`bytes與corrupted plan回`rewriteVerificationFailed`全綠。

- [x] 2.7 依「Decision 9：truth-table synthesis 使用 deterministic balanced DNF，再轉為 NOR 並自我驗證」實作canonical true-row minterms、pairwise balanced and／or folds、全假contradictions與全真tautologies都語法引用全部table atoms、DNF與rewrite雙重estimator、NOR-only materialization及canonical atom array＋output vector self-check；self-check只接受module-internal per-call fault injector且public固定no-op；驗證：0-arity typed拒絕、16個binary functions、bit-order permutation、三原子single／`{1,3,6}`multirow與constants exact plan/final bytes、depth65、五原子4,095成功／4,103拒絕、exact unary-not synthesis bytes與mismatch seam全綠。

- [x] 2.8 依「Decision 10：測試與 mutation evidence 是實作契約的一部分」、「Traversal and storage rules」及「Production file ownership」補齊module-internal per-call `EnumerationWorkspaceProbe`、injectable checked-shift helper、per-call atomic observer、含atom table的deterministic `structuralHashFeed`、rewrite／synthesis fault injectors與bounded iterative node tables，並確認production symbol落在指定檔案、無global mutable seam、`2^n` cardinality path無第二個裸shift（已guard的row-bit decoding除外）；驗證：13／63-row與13-completion拒絕時shift／allocator invocation皆為0、重複atom evaluation count為1、hash feed含atom bytes／tag／boundary／index，4,096-node equality與trace equality不耗盡遞迴堆疊。

## 3. 遷移與文件

- [x] 3.1 執行 source-breaking production遷移：`Projection.swift` 不再 pattern-match public raw cases、`Question.swift` 儲存已驗證 subject與reserved negation、`Proposition.evaluate(in:)` 改走 `try asExpression()`，移除 ambiguous root `trace.atomic`／`scope`／`projection`／`evidence`／`snapshotQuarantine` proxies並只保留一版 deprecated unary `operand` view；驗證：所有 production targets在 `swift build -Xswiftc -warnings-as-errors` 下編譯，non-`@testable` fixture只能使用 throwing factories，且程式碼搜尋不存在第二套 `Formula`、`Negation.swift` AST、strong-Kleene evaluator或 public `limit`／`budget` 參數。

- [x] 3.2 依「Required regression preservation」遷移 `NegationTests`、`AuthorshipCompletenessPropositionTests`、`ContextValuationTests`、`PropositionTests` 與下游 AkashicKit 呼叫端：把 `p.expression`、enum-style `.atom`／`.not`、raw case matching與 root trace proxy改為 throwing factory及 `children`／`atomicEvidence` traversal；驗證：#205 malformed／empty boundary、#212 snapshot ID／revision／valid day、#213 undetermined reason／negative-answer trace與 authored／affiliated projection、evidence、completeness、temporal suites結果皆不變。

- [x] 3.3 更新public API doc comments與遷移說明，明列opaque construction、固定limits、classical／supervaluation分層、完整Unicode 15.1.0 trust chain與offline regeneration、source-breaking移除項、deprecated wrappers及`maximumOperatorDepth` alias、零元function table不可synthesis及`Mirror`不在automatic rendering保證內；驗證：所有symbol、limit、UCD檔名、digest欄位與domain由編譯測試或golden fixture引用，並確認未宣稱semantic normalization、hash collision-free、caller可調上限或跨型別table bytes相等。

## 4. Mutation evidence 與最終閘門

- [x] 4.1 執行expression／Unicode／classical mutation批次：逐一暫改double-negation、child swap、移除hash feed atom payload／tag／boundary、各limit off-by-one、改收raw dictionary或duplicate first-wins、raw／syntax／unsupported／streaming normalized順序、同改manifest與data但不升domain、改generator／runtime／generated digest、回用Foundation／CoreFoundation White_Space／NFC、把row guard移到checked shift後、truth cell／bit significance／permutation／domain／六node tags／length／row-index與equivalence；驗證：指定locators每次由綠轉紅，記錄symbol／command／failed assertion，復原後全綠且不殘留mutation。

- [x] 4.2 執行 supervaluation／trace／question mutation批次：逐一改回 strong Kleene、移除 absorbing case、短路任一 child、反轉 children、覆寫 context／root conclusion、對 duplicate occurrence重投射、移除completion preflight及讓no-answer重評 atom；驗證：`SupervaluationTests`、`NegationTests`與question／adjudication regressions的指定 locator每次由綠轉紅，evidence記錄完整，復原後所有 context、refusal、quarantine與occurrence trace assertion再度全綠。

- [x] 4.3 執行rewrite／synthesis mutation批次：逐一留下非NOR node、改錯每條identity、移除rewrite depth／node或synthesis depth／node guard、先materialize再檢查、繞過或忽略兩個fault injectors／verifiers、反轉row order、漏掉任一binary mask、破壞全真／全假全部-atoms construction、重映射rewrite／synthesisgolden tag及改用不穩定iteration；驗證：對應locator逐一由綠轉紅並留下symbol／command／failed assertion，復原後verification failures、16 masks、depth65、4,095／4,097／4,103與canonical replay全綠。

- [x] 4.4 依「Verification gates」執行四個新增 suites、四個遷移 suites、完整 Swift test suite、`swift build -Xswiftc -warnings-as-errors`、fresh-subprocess canonical replay與 strict Spectra validation；驗證：所有命令零失敗、mutation紀錄完整、兩份 delta specs／proposal／design／tasks一致，並只在取得可重播的 production symbol與test locator後把 #214 landed evidence交給 #216 corpus disposition。
