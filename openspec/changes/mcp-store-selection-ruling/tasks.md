## 1. 橫切選項的裁決表

- [ ] 1.1 落實「橫切選項需要自己的裁決表」：`.claude/rules/mcp-cli-parity.md` 出現第三張
      裁決表，行為「CLI 橫切選項」，恰兩列——`--library`（`LibraryOptions`）與
      `--config`（`FileConfigOptions`）。每列都給出**具名的裁決**（不得是「未決」——
      該檔明文規定未決不是選項）與一句理由；欄位結構與該檔既有兩張表同構。
      驗證：逐列讀表，確認兩列各有非「未決」的裁決值與理由；與既有兩表的欄位標題並排
      對照，欄位語意一致。
- [ ] 1.2 `--library` 那一列的裁決內容即「MCP 不新增 per-invocation 的 store 參數」，
      並在理由中寫出它依賴的三個前提：MCP 已有與 App 同形的 session 級選擇、per-call
      選填參數對 LLM 消費端會靜默寫錯、`library` 這個名字在 MCP 已被佔用。
      驗證：讀該列理由，三個前提都能找到；且理由中不出現「MCP 沒有 store 選擇」這類
      與事實相反的敘述。
- [ ] 1.3 `--config` 那一列的裁決內容即「--config 同為有理由的缺席」，理由寫明
      registry 檔位置是部署層決定，MCP server 的啟動環境已決定它讀哪一份 registry。
      驗證：讀該列理由，確認論據是部署層而非沿用 1.2 的三個前提（兩列理由不得雷同）。
- [ ] 1.4 該表明寫封閉性與「不得依性質相似類推」，使讀者不能拿判準自行推導出未寫下的
      裁決（全域 `common-spec-prose-enumeration` 的要求）。
      驗證：在該節文字中找到封閉性宣告句與「不得依性質相似類推」字樣。

## 2. 稽核程序的橫切枚舉步驟

- [ ] 2.1 落實「稽核程序增加橫切型別的枚舉步驟」：該檔的機械檢查一節出現一個新步驟，
      形式與既有步驟一致——一條可直接貼進終端機執行的命令，外加一句「輸出中每一項都
      必須出現在橫切選項表」的判定說明。
      驗證：把該命令貼進終端機執行，輸出恰為 `LibraryOptions` 與 `FileConfigOptions`
      兩項，且兩者都能在任務 1.1 的表中找到對應列（零差集）。
- [ ] 2.2 該步驟附一句邊界註記：它枚舉的是 `ParsableArguments` 的**宣告式**，不是所有
      可能的橫切機制，因此存在假陰性的可能——這與該檔既有稽核程序踩過的坑
      （型別命名慣例的 regex 只命中 11/30）同型，必須寫出來而非假裝沒有。
      驗證：該註記存在，且明說假陰性方向（漏報而非誤報）。

## 3. 供 #298 引用的事實記錄

- [ ] 3.1 落實「MCP 的 store 是 session 狀態」：該檔內存在一則可被引用的事實記錄——
      MCP 面的 active store 是 session 狀態，可在 session 中途經 `akashic_files` 的
      `use` action 改變，且呼叫端不會在每次呼叫時重新宣告它；因此任何以「本次呼叫有
      沒有顯式指定 store」為判準的閘，在 MCP 面恆為否，不可作為判準。
      驗證：僅閱讀該規則檔（不回頭讀 `Sources/` 任何檔案）即可得出「#298 的閘不能用
      `--library` 顯式性當判準」這個結論——以「該段是否自足」為標準人工審閱。
- [ ] 3.2 該檔記錄 `library` 這個名字在兩面意義不同（CLI＝store root 路徑；
      MCP＝store 內的 membership 分類），並交叉指向 #315。
      驗證：該註記存在且含 #315 的指向；與 `Sources/akashic-mcp/Server.swift` 內
      `library` 參數的 description 文字對照，語意描述一致。

## 4. 非回歸

- [ ] 4.1 該檔既有的兩張裁決表與兩條機械檢查程序**逐字未被改動**——本 change 是新增
      一張表與新增一個稽核步驟，不重寫既有內容。
      驗證：對該檔取 diff，只顯示新增區塊；既有表格區段與既有稽核程序區段的 diff 為零。
- [ ] 4.2 既有兩條稽核程序的結論不變：MCP 面枚舉出的工具全數在 MCP 表中有列，CLI 面
      枚舉出的 subcommand 全數出現在 MCP 表的「CLI 對應」欄或 CLI-only 表之一。
      驗證：執行該檔既有的兩條枚舉命令，把輸出與兩張表對照，兩個方向皆零差集。
