# IDD tree-lock (idd-tree-lock.sh #183) — per-machine session state, not tracked
.claude/.idd/tree-lock

# IDD 附件快取（diagnose 產物，per-clone）
.claude/.idd/attachments/

# Swift build artifacts
.build/
.swiftpm/
*.xcodeproj
DerivedData/

# macOS
.DS_Store

# 使用者資料（獨立 private repo / 外部 pool，不進 umbrella git 歷史）
library/
attachments/
mcpb/server/
mcpb/*.mcpb
mcpb/*.sha256

# Xcode
AkashicApp/DerivedData/

# IDD runtime state（unattended-state lib，不入版控）
.claude/.idd/state/

# 背景 verify agent 的 probe 檔（#36 verify 期間三度被 `git add -A` 掃進 commit）。
# agent 會就地建檔以便 `swift test` 跑得到，主 session 的 `add -A` 就把它們一起收了。
# 命名慣例：ZZ 前綴（排在檔案清單最後、一眼看得出是暫時的）。
Tests/**/ZZ*.swift
.spectra/
openspec/.vector-search.db*

# IDD parallel worktree isolation (idd-worktree.sh #167) — not tracked in main tree
.claude/worktrees/

# IDD per-clone config（legacy 形式；現行 .claude/.idd/local.json 照常追蹤）
.claude/issue-driven-dev.local.*

# Python 建置產物（#407 R40）。守衛與 negative control 是 .py，而 harness 會被
# import（probe／測試），於是 `git add -A` 會把 `__pycache__/` 一起掃進去——
# 實地發生過：`plugin/tests/__pycache__/audit-guards-mutations.cpython-313.pyc`
# 被 R39 的 commit 帶上版控。
__pycache__/
*.pyc

# #516：階段 B 摘要 → [Proposal] 的中間檔裝第三方逐字摘要（含版權聲明），不進版控
proposals*.json
